/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'image.tmdb.org',
      'vz-cdn.b-cdn.net',
      'iframe.mediadelivery.net',
    ],
  },
}

module.exports = nextConfig
